# Guess-game
A simple and interactive number guessing game built using HTML, CSS, and JavaScript. The user tries to guess a randomly generated number, and the app gives hints such as "Too High" or "Too Low" until they guess correctly.
